import ReactLoading from 'react-loading';
const Loading = () => {

    return (
        <ReactLoading type='bubbles' color='#ec4e36' height={80} width={80} className="loading-bar" />
    )
}

export default Loading